# Solicitar al usuario que ingrese un número real
numero_real = float(input("Ingrese un número real: "))

# Extraer la parte decimal utilizando el operador %
parte_decimal = abs(numero_real % 1)

# Mostrar la parte decimal
print(parte_decimal)
